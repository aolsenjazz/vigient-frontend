import React from 'react';
import FullScrapeConfig from './FullScrapeConfig'; // Import the FullScrapeConfig component
import HeadScrapeConfig from './HeadScrapeConfig'; // Import the HeadScrapeConfig component

type JobsConfigurationParams = {
  jobType: string;
};

const JobConfiguration = ({ jobType }: JobsConfigurationParams) => {
  return null; // for now
};

export default JobConfiguration;
